{
    "name": "cooperaj/bolt-dokku-config",
    "description": "Takes configuration information made available in the environment by dokku and writes out a config_local.yml configuration file so Bolt can make use of it.",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Adam Cooper",
            "email": "adam@networkpie.co.uk"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "symfony/filesystem": "^2.8"
    },
    "require-dev": {
        "phpunit/phpunit": "^6.1"
    },
    "autoload": {
        "psr-4": {
            "BoltEnv\\": "src/"
        }
    }
}
